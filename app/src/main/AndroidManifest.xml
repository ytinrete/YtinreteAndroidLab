<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.ytinrete.android.lab"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>


    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- for umeng -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>


    <application
        android:name="com.ytinrete.base.YtApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- splash -->
        <activity
            android:name="com.ytinrete.splash.YtSplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- main -->
        <activity
            android:name="com.ytinrete.main.YtMainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|layoutDirection"
            android:theme="@style/MainTheme">
        </activity>

        <!-- exp -->
        <activity
            android:name="com.ytinrete.exp.YtExpActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|layoutDirection">
        </activity>

        <!-- music -->
        <activity
            android:name="com.ytinrete.music.view.YtMusicActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait"
            android:taskAffinity="com.ytinrete.android.lab.music_screen"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <service
            android:name="com.ytinrete.music.service.YtMusicService"
            android:enabled="true"/>

        <!-- multi process -->
        <provider
            android:name="com.ytinrete.multiproccess.MtpMasterProvider"
            android:authorities="com.ytinrete.content"
            android:exported="false"/>

        <activity
            android:name="com.ytinrete.multiproccess.MtpMasterActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|layoutDirection"
            android:screenOrientation="portrait"/>

        <service
            android:name="com.ytinrete.multiproccess.MtpSlaveService"
            android:exported="false"
            android:process=":slave">
            <intent-filter>
                <action android:name="com.ytinrete.android.lab.aidl"/>
            </intent-filter>

        </service>

        <service
            android:name="com.ytinrete.multiproccess.MtpSlaveMessengerService"
            android:exported="false"
            android:process=":slave">

        </service>


        <activity
            android:name="com.ytinrete.multiproccess.MtpSlaveActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|layoutDirection"
            android:process=":slave"
            android:screenOrientation="portrait"/>


    </application>

</manifest>